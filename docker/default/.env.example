# Optional API backends; set only the keys you use.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
LLAMA_API_KEY=
ROS_DOMAIN_ID=0
# Leave these disabled for the first online run. Enable them only after the
# workspace-root hf_cache, hf_models, and .clip directories are populated.
HF_HUB_OFFLINE=0
TRANSFORMERS_OFFLINE=0
